<!-- Responsive navbar-->
<!-- menu navbar placé dans un header pour améliorer le SEO qui prend en compte la sémantique -->
<header>
    {# suppression de la marge basse du header lord de l'apparition d'un flashMessage (vérifié avec peekAll pour ne pas perdre le flashbag à la lecture #}
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary {{ app.session.flashbag.peekAll()|length > 0 ? '' : 'mb-1' }}">
        <div class="container px-lg-5">
            <a class="navbar-brand" href="{{ path("app_home") }}">7lab</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation"><span class="navbar-toggler-icon"></span></button>
            <div class="collapse navbar-collapse" id="navbarSupportedContent">
                <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
                    <li class="nav-item"><a class="nav-link" aria-current="page" href="{{ path("app_home") }}">Liste des voitures</a></li>
                    <li class="nav-item"><a class="nav-link" href="{{ path("app_voiture_new") }}">Ajouter une voiture</a></li>
                    <li class="nav-item"><a class="nav-link" href="{{ path("app_constructeur_index") }}">Liste des constructeurs</a></li>
                    <li class="nav-item"><a class="nav-link" href="{{ path("app_constructeur_new") }}">Ajouter un constructeur</a></li>
                </ul>
            </div>
        </div>
    </nav>
</header>