{% extends 'base.html.twig' %}

{% block title %}Voiture{% endblock %}

{% block body %}
    <h1>Voiture</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ voiture.id }}</td>
            </tr>
            <tr>
                <th>Model</th>
                <td>{{ voiture.model }}</td>
            </tr>
            <tr>
                <th>Length</th>
                <td>{{ voiture.length }}</td>
            </tr>
            <tr>
                <th>Width</th>
                <td>{{ voiture.width }}</td>
            </tr>
            <tr>
                <th>Weight</th>
                <td>{{ voiture.weight }}</td>
            </tr>
            <tr>
                <th>Seat</th>
                <td>{{ voiture.seat }}</td>
            </tr>
            <tr>
                <th>Energy</th>
                <td>{{ voiture.energy }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_voiture_index') }}">back to list</a>

    <a href="{{ path('app_voiture_edit', {'id': voiture.id}) }}">edit</a>

    {{ include('voiture/_delete_form.html.twig') }}
{% endblock %}
